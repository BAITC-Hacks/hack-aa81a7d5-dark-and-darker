# AI Sana Challenge Hub

Рабочий MVP для HackAlem AI: бизнес оформляет и публикует задачи, студенческие
команды предлагают решения, представитель бизнеса вручную принимает или
отклоняет предложения. Интерфейс на русском языке, с демонстрационными ролями.

## Технологии

- React 19, TypeScript, Vite; HTTP-запросы через `fetch`.
- Python 3.14.3, FastAPI, Pydantic v2, Uvicorn в локальном `.venv`.
- SQLite через стандартный модуль `sqlite3`, файл `backend/hackalem.db`.
- OpenAI Responses API через официальный Python SDK: индивидуальные вопросы и карточки.
  Модель берётся из `OPENAI_MODEL` (для проекта — `gpt-5.4`).
- Детерминированный рейтинг и резервный режим при недоступности AI.
- Playwright — только для браузерных тестов, не нужен приложению во время работы.

Проект находится непосредственно в `/Users/altair/HackAlem`. Проверенная среда:
macOS, Node.js 22.22.1, npm 10.9.4, Python 3.14.3. Версии зависимостей
зафиксированы в `package-lock.json` и `backend/requirements.txt`.

## Установка

```sh
cd /Users/altair/HackAlem
python3 -m venv .venv
.venv/bin/python -m pip install -r backend/requirements.txt
npm ci
```

Если окружение и зависимости уже установлены, повторять установку не нужно.
Никакие зависимости не устанавливаются глобально. Без настройки OpenAI приложение
продолжает работать в резервном режиме.

## Настройка OpenAI

Настройки читаются **только backend** из `backend/.env` через `python-dotenv`.
В файле нужны `OPENAI_API_KEY` и `OPENAI_MODEL`. Образец с пустым ключом находится
в `backend/.env.example`; существующий `backend/.env` не перезаписывайте.
Укажите собственный ключ в этом локальном файле и задайте `OPENAI_MODEL=gpt-5.4`.
Ключ не передаётся frontend, не включается в prompts и ответы API.
Не используйте переменные с префиксом `VITE_` для ключа.

Переменные окружения процесса имеют приоритет над файлом, включая явно пустые
значения. После изменения настроек **перезапустите backend**: настройки кэшируются.
Код не подменяет недоступную модель другой моделью.

Реализация использует `client.responses.parse(..., text_format=PydanticModel)`
с JSON-схемой, `store=False`, тайм-аутом 45 секунд и `max_retries=0`.
Генерация ограничена 6 000 выходных токенов, reasoning effort — `low`.
См. [Responses Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs)
и [GPT-5.4](https://developers.openai.com/api/docs/models/gpt-5.4).
Браузер ждёт AI-endpoint максимум 55 секунд, затем использует локальный резерв.

В OpenAI отправляются только введённые для генерации название, описание, вопросы
и ответы. Запросы выполняются по нажатию кнопок конструктора; каталог, рейтинг,
публикация, команды и предложения не вызывают OpenAI.

При отсутствии ключа/модели, отказе авторизации, недоступной модели, лимите,
тайм-ауте, ошибке сети, отказе генерации или некорректном JSON возвращается
рабочий результат с `source="fallback"`, безопасным кодом `reason` и сообщением
на русском. Приложение не выводит исходные ошибки провайдера и секреты в ответы
или логи. В интерфейсе появляется сообщение **«Включён резервный режим»**:
семь стандартных вопросов, затем карточка из исходных ответов. Публикация остаётся
доступной по прежним правилам.

Если OpenAI отклоняет авторизацию, проверьте действительность ключа и его проект
в своём аккаунте OpenAI, обновите локальный файл и перезапустите backend.
При `model_unavailable` проверьте доступ проекта к GPT-5.4; приложение сохраняет
выбранную модель и работает в резервном режиме.

`backend/.env` исключён из Git. Проверка без раскрытия содержимого:

```sh
git check-ignore -v backend/.env
git ls-files -- backend/.env
```

Вторая команда не должна выводить путей. Не добавляйте файл через `git add -f`.

## Запуск на macOS

Первый терминал — backend с перезагрузкой при изменениях:

```sh
cd /Users/altair/HackAlem
npm run dev:backend
```

Эквивалентная команда без npm:

```sh
.venv/bin/python -m uvicorn backend.app.main:app --reload --reload-dir backend --host 127.0.0.1 --port 8000
```

Второй терминал — frontend:

```sh
cd /Users/altair/HackAlem
npm run dev
```

- Приложение: http://127.0.0.1:5173
- Документация API: http://127.0.0.1:8000/docs
- Проверка доступности: http://127.0.0.1:8000/api/health

Остановка серверов — `Ctrl+C` в соответствующем терминале.
Vite проксирует `/api` на `http://127.0.0.1:8000`; CORS для этого сценария не нужен.
Если порт занят, сначала остановите ранее запущенный сервер.

## SQLite и тестовые данные

Таблицы и демонстрационные данные создаются автоматически при первом запуске
backend. Явная инициализация:

```sh
cd /Users/altair/HackAlem
npm run seed
```

Seed использует транзакцию и запись `demo-v1` в таблице `seed_runs`.
Повторный запуск не создаёт дубликаты и не перезаписывает пользовательские данные.

Исходные данные:

- 1 бизнес: Sana Business Lab, представитель Айдана Садыкова.
- 5 первоначальных черновиков.
- 5 опубликованных карточек: отзывы клиентов (100), спрос (75), доставка (55),
  заявки (30), энергопотребление (10).
- 5 команд с описаниями, навыками и контактами.
- 7 предложений: 3 на рассмотрении, 2 принятых, 2 отклонённых.

Все организации, контакты, количественные показатели и предложения в seed —
вымышленные демонстрационные данные. Конструктор новых задач получает только
введённый пользователем текст. Пустые ответы принудительно остаются пустыми
в AI-карточке; смысл непустых формулировок нужно проверить перед подтверждением.

База сохраняется между перезапусками. Не удаляйте `backend/hackalem.db`, если
нужно сохранить работу. Для отдельной базы можно передать переменную окружения:

```sh
HACKALEM_DB_PATH=/absolute/path/demo.db npm run dev:backend
```

Каталог назначения должен существовать. Базы `*.db`, их служебные файлы,
`.venv`, `node_modules`, `.env` и результаты сборки исключены через `.gitignore`.

## Демонстрационные роли

Переключатель **Бизнес / Студент** находится в шапке.

**Бизнес:** свои задачи, создание и редактирование, подтверждение, публикация,
рейтинг и входящие предложения с ручным решением.

**Студент:** только опубликованные задачи в каталоге, профили команд, выбор
демонстрационной команды, отправка и просмотр её предложений.
Выбор команды доступен в профиле, форме предложения и разделе «Мои предложения».

Роль и выбранная команда могут запоминаться в localStorage. Все задачи, команды,
предложения и решения хранятся в SQLite и загружаются из API.

## Конструктор и рейтинг

1. Название и первоначальное описание создают черновик.
2. При первом переходе «Далее» AI готовит семь индивидуальных вопросов;
   при недоступности AI используются стандартные. Ответы необязательны.
3. При первом нажатии «Сформировать карточку» AI редактирует ответы по структуре;
   в резервном режиме карточка собирается из них без переписывания.
4. Любое поле можно отредактировать, затем нажать «Подтвердить карточку».

Ответы сохраняются в SQLite при переходе вперёд и назад. Кнопка «Сохранить и
выйти» сохраняет текущие изменения. Закрытие вкладки с несохранёнными изменениями
вызывает предупреждение браузера. Перед переходом в другой раздел сохраните форму.


Возврат между уже заполненными шагами **не вызывает повторную генерацию**.
Для нового запроса нужны кнопки «Сгенерировать вопросы заново» или
«Сформировать карточку заново». Первая сохраняет ответы, вторая заменяет текст
карточки результатом генерации на основе исходных ответов. При изменении названия
или описания вопросы не обновляются автоматически — об этом напоминает интерфейс.

В открытом конструкторе исходные ответы хранятся отдельно от AI-редакции:
переход назад показывает введённые ответы, дальнейший переход сохраняет
уже сделанные ручные правки карточки. Если изменить ответ, его новая формулировка
переносится в соответствующее поле карточки без автоматического AI-запроса.
SQLite хранит текущую сохранённую карточку. После закрытия/перезагрузки конструктора
редактор открывает её; отдельной постоянной истории исходных ответов и AI-вопросов нет.

AI не получает и не назначает рейтинг или статус публикации. Backend проверяет
JSON через Pydantic, требует все девять полей карточки, сохраняет точный контакт,
восстанавливает непустой ответ, если модель его стёрла, и очищает разделы без
ответов. Для пустых разделов сведения не переносятся из первоначального описания:
внесите их в соответствующий ответ вручную. Проверка смысла остаётся за бизнесом.

Рейтинг рассчитывает backend:

| Поле | Баллы |
| --- | ---: |
| Контекст и потребность | 20 |
| Данные и материалы | 20 |
| Ожидаемый результат | 15 |
| Критерии успеха | 15 |
| Ограничения | 10 |
| Пользователи | 10 |
| Связь с бизнесом | 10 |

Поле после удаления крайних пробелов непустое — начисляется весь вес, иначе 0.
Название и краткое описание не добавляют баллов. Это оценка полноты заполнения,
а не автоматическая проверка смысла или достоверности текста.

Уровни: 0–39 «Черновик», 40–69 «Рабочая», 70–89 «Готовая», 90–100 «Приоритетная».
Уровень «Черновик» — характеристика рейтинга; статус публикации хранится отдельно.

**Рейтинг не блокирует публикацию, в том числе при 0 баллов.** Обязательно лишь
ручное подтверждение текущей версии. Сохранение содержательных изменений
сбрасывает подтверждение и переводит опубликованную задачу обратно в черновик.
После повторного подтверждения её можно опубликовать снова. Уже отправленные
предложения сохраняются. Сохранение тех же значений не сбрасывает подтверждение.

Команда может отправить одно предложение на задачу. Новое предложение получает
статус `pending`. Только человек принимает или отклоняет его. Обработанное
предложение нельзя обработать повторно или вернуть в `pending`; API возвращает
409. Для ожидающего предложения запрос `pending` оставляет его без изменения.
Принятие одного предложения не отклоняет другие автоматически.

Резервные вопросы и прежний расчёт рейтинга находятся в `backend/app/readiness.py`.
Интеграция OpenAI выделена в `backend/app/ai.py`; операции с SQLite и предложениями
не зависят от доступности AI.

## API

Полные схемы запросов доступны в `/docs`.

| Метод | Путь | Назначение |
| --- | --- | --- |
| GET | `/api/health` | Доступность сервера |
| GET | `/api/questions` | Локальные вопросы и веса полей |
| POST | `/api/ai/questions` | Семь индивидуальных вопросов или резервный набор |
| POST | `/api/ai/task-card` | Структурированная карточка или исходные ответы |
| GET | `/api/tasks` | Каталог с фильтрами, поиском и сортировкой |
| GET | `/api/tasks/{id}` | Карточка задачи |
| POST | `/api/tasks` | Создание черновика, HTTP 201 |
| PATCH | `/api/tasks/{id}` | Изменение полей карточки |
| POST | `/api/tasks/{id}/confirm` | Подтверждение текущей версии |
| POST | `/api/tasks/{id}/publish` | Публикация подтверждённой карточки |
| GET | `/api/tasks/{id}/readiness` | Баллы, уровень и рекомендации |
| GET | `/api/teams` | Список команд |
| GET | `/api/teams/{id}` | Профиль команды |
| POST | `/api/tasks/{id}/proposals` | Предложение команды, HTTP 201 |
| GET | `/api/tasks/{id}/proposals` | Предложения по задаче бизнеса |
| GET | `/api/teams/{id}/proposals` | Предложения выбранной команды |
| GET | `/api/proposals` | Все предложения демонстрационному бизнесу |
| PATCH | `/api/proposals/{id}/status` | Ручное решение по предложению |

Параметры `/api/tasks`:

- `status`: `draft` или `published`.
- `readiness_level`: `Черновик`, `Рабочая`, `Готовая`, `Приоритетная`.
- `sort`: `newest`, `oldest`, `score_desc`, `score_asc`.
- `search`: поиск по названию и описанию без учёта регистра, включая русский.
- `business_id`: фильтр по владельцу; демонстрационный бизнес имеет ID 1.

Ошибки: 404 — объект не найден; 422 — неверные входные данные; 409 — конфликт
состояния, неподтверждённая публикация, повторное предложение или решение;
403 — изменение чужой задачи. Изменяющие запросы возвращают актуальный объект.

AI-endpoints не сохраняют и не публикуют задачи самостоятельно:

- `/api/ai/questions`: вход `title`, `initial_description`; ответ `questions`
  (ровно семь объектов `field`, `question`), `source`, `reason`, `message`.
- `/api/ai/task-card`: вход `title`, `initial_description`, `questions` и `answers`
  (объект с семью строковыми полями критериев); ответ `card` из девяти полей,
  `source`, `reason`, `message`. Пустые ответы передаются как `""`.
- Ошибки входных данных возвращают 422. Сбой OpenAI возвращает **200 с резервным
  результатом**, `source="fallback"` и причиной; при успехе — `source="ai"`.
- Коды причин: `missing_api_key`, `missing_model`, `authentication`,
  `model_unavailable`, `rate_limit`, `timeout`, `connection`, `invalid_response`,
  `provider_error`. Текст исходной ошибки OpenAI не возвращается.

## Сценарий демонстрации

1. Откройте приложение в роли **Бизнес**. Посмотрите черновики и опубликованные задачи.
2. Нажмите **Создать задачу**, введите название и описание, перейдите дальше.
3. Ответьте на несколько вопросов; часть полей оставьте пустыми. Проверьте, что
   ответы сохраняются при переходах «Назад» / «Далее».
4. Просмотрите структуру, перейдите к правкам, измените любое поле и подтвердите.
5. Посмотрите баллы и рекомендации. Опубликуйте задачу даже при низком рейтинге.
6. При желании отредактируйте её: подтверждение сбросится, публикацию понадобится повторить.
7. Переключитесь на **Студент**, найдите задачу в каталоге. Попробуйте поиск,
   фильтры и сортировку. Посмотрите профили команд.
8. Откройте задачу, выберите команду, заполните сообщение и решение, отправьте
   предложение. Убедитесь в статусе «На рассмотрении» в «Мои предложения».
9. Переключитесь на **Бизнес**, откройте «Предложения», примите или отклоните его.
10. Вернитесь к студенту, проверьте новый статус. Перезапустите backend — данные сохранятся.

## Проверки

```sh
cd /Users/altair/HackAlem
npm run build
npm run test:backend
```

Backend-тесты используют стандартный `unittest`, отдельную временную SQLite-базу
и настоящий HTTP-сервер на свободном порту. Они проверяют 128 комбинаций рейтинга,
seed, валидацию, поиск и сортировку, публикацию с 0 баллов, сброс подтверждения,
предложения и сохранность данных после остановки и повторного запуска сервера.

Для браузерных тестов один раз установите тестовый браузер:

```sh
npx playwright install chromium
npm run test:e2e
```

E2E-тесты автоматически поднимают отдельные серверы на портах 5174 и 8001,
создают временную базу и удаляют её после проверки. Демонстрационная база
`backend/hackalem.db` не меняется. Проверяются полный сценарий, нулевой рейтинг,
фильтры, команды, принятие и отклонение, мобильный экран и восстановление после
ошибки сети. Скриншоты и диагностические данные находятся в `test-results/`.

AI-тесты используют подменённые ответы SDK и mock HTTP transport: проверяют
структурированную схему Responses API, все семь критериев, некорректные ответы,
тайм-ауты, лимиты, недоступность модели, сохранение ответов и сокрытие секретов.
Пакет `httpx2` для этих тестов устанавливается как зависимость официального SDK.

Дополнительные браузерные тесты проверяют AI-загрузку, отсутствие повторных вызовов
при переходах, сохранение исходных ответов и ручных правок, явную повторную
генерацию и публикацию после сбоя AI. **Во всех автоматических тестовых серверах
OpenAI отключён пустыми переменными окружения**; платных сетевых запросов нет.

При разработке выполнена одна реальная попытка генерации вопросов с настройками
`backend/.env`: OpenAI вернул отказ авторизации (`authentication`), проверен
резервный результат из семи вопросов. Реальная генерация карточки после отказа
не запускалась; модель и ключ не изменялись. Успешная реальная генерация требует
исправления доступа OpenAI и ручной проверки пользователем.

Локальный просмотр production-сборки:

```sh
npm run build
npm run preview
```

Откройте http://127.0.0.1:4173. Backend должен быть запущен отдельно.

## Структура и файлы реализации

```text
backend/app/main.py           REST API и переходы состояний
backend/app/ai.py             OpenAI Responses API и резервный режим
backend/app/ai_models.py      Структуры запросов и AI-ответов
backend/app/config.py         Чтение backend/.env
backend/app/db.py             SQLite, транзакции и схема
backend/app/models.py         Валидация входных данных Pydantic
backend/app/readiness.py      Рейтинг и локальные вопросы
backend/app/seed.py           Идемпотентные демонстрационные данные
backend/tests/test_api.py     Unit- и HTTP-тесты backend
backend/tests/test_ai.py      AI-endpoints с mock-ответами и тест SDK-контракта
frontend/src/main.tsx         Точка входа React
frontend/src/App.tsx          Роли, навигация, уведомления
frontend/src/api.ts           Запросы и загрузка данных
frontend/src/ai.ts            AI-запросы и локальный резерв
frontend/src/context.tsx      Общий UI-контекст
frontend/src/types.ts         Типы данных
frontend/src/components/      Каталог, карточка, конструктор, команды, предложения
frontend/src/styles.css       Адаптивный интерфейс
frontend/tests/hub.spec.ts    Сквозные браузерные тесты
frontend/tests/ai.spec.ts     Браузерные проверки AI-конструктора
scripts/test-e2e.mjs          Изоляция базы браузерных тестов
playwright.config.ts         Настройки браузерных тестов
vite.config.ts               Vite и API-прокси
```

## Границы MVP

Регистрации и авторизации по заданию нет. Переключатель ролей управляет
демонстрационным интерфейсом, а не защищает API. Сервер рассчитан на локальную
демонстрацию с одним бизнесом; для публичного развёртывания нужны реальные
пользователи, авторизация и проверка доступа к данным.

Нет загрузки файлов, автоматического назначения исполнителей,
отправки писем, пагинации и совместного редактирования с разрешением конфликтов.
Контакты — текст. Проверка качества описания остаётся за человеком.
